pipeline {
  agent none

  stages {
    stage('CheckOut') {
      agent { label ('local') }
      steps {
        git branch: 'main',
        url: 'https://github.com/miju61/source-maven-java-spring-hello-webapp.git'
      }
    }
    stage('Build') {
      agent {
        docker { image 'maven:3-openjdk-17' }
      }
      steps {
        sh 'mvn clean package'
      }
    }
    stage('ImageBuild') {
      agent { label ('local') }
      steps {
        sh 'docker image build -t tomcat:hello .'
      }
    }
    stage('ImageTag') {
      agent { label ('local') }
      steps {
//        sh 'docker image tag tomcat:hello leemiju/tomcat:$BUILDNUMBER'
        sh 'docker image tag tomcat:hello leemiju/tomcat:latest'
      }
    }
    stage('ImagePush') {
      agent { label ('local') }
      steps {
        withDockerRegistry(credentialsId: 'docker-hub-token', url: 'https://index.docker.io/v1/') {
//          sh 'docker image push leemiju/tomcat:$BUILDNUMBER'
          sh 'docker image push leemiju/tomcat:latest'
        }
      }
    }
    stage('Running Container') {
      agent { label ('local') }
      steps {
        ansiblePlaybook(playbook: 'playbook.yaml')
      }
    }
  }
}
